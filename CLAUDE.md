# お家トレ（ouchitore）— このリポジトリの決まり

自宅でやったトレーニングを、その場で残しておくための記録帳。サーバーを持たない静的サイトで、記録は利用者の端末（IndexedDB）にだけ残る。**ここは公開して配るためのリポジトリ**で、ここに置いたものは全部そのまま人目に触れる。

もとは個人の記録を扱う別プロジェクトの中の一フォルダだった。**2026-09-11 に本人の判断で家を分けた**＝公開のたびに「出していいものだけを切り出す」作業が要るのは、母屋の中に置いているからだった。いまは切り出しが要らない。

## 絶対に守ること

- **個人データを1バイトも置かない。** 日記・体調・家計・位置・認証情報・実際の記録ファイル。ここは他人の端末に配られる。
- **外部へ通信を増やさない。** いまの外向きは2つだけ＝(1) 動画のサムネイル `i.ytimg.com`（利用者のブラウザが直接読む・保存しない）、(2) 貼られた URL のタイトルを取る YouTube oEmbed。**これ以外を足すときは、なぜ要るかを README に書いてからにする。** 解析タグ・外部フォント・CDN は入れない。
- **点数をつけない。順位をつけない。連続日数を数えない。励まさない。** 事実だけを淡々と出す。これは思想であって好みではない（[README](README.md) の「しないこと」）。
- **色相だけで意味を伝えない。** 作者は色弱。形・枠・明度差を必ず併用する。
- **比喩や造語で機能を呼ばない。** 「種目の一覧」は一覧であって「庫」ではない。
- **相棒に名前を付けない。** 絵だけ。既存の語・人名・名字と重ならない造語を探して二度失敗した（2026-09-11）。読み上げ用の `alt` と `aria-label` にだけ動物名が入る。

## 見た目

レイアウト・配色・部品・遷移・コピーは **Claude Design** が決める。ここでコードを書く側が画面を自作しない（毎回ダサくなる）。決まった見た目は `design/` のアートボードにある。寸法の制約（指で押せる大きさ等）は要件として Design に渡す。

## 変更のしかた

1. `index.html` の `?v=NN` を**必ず**上げる。上げないと、ホーム画面に追加した端末が古い版を持ち続ける。
2. データ層（`store.js`）を触ったら回帰テストを通す。

```
node tools/test_store.js
```

3. データ層を触ったら、10人分の使い方も流す（数週間ぶんの日付を一気に。実時間では出ない種類のズレが出る）。

```
node tools/simulate.js          # 10人 x 8週
node tools/simulate.js 52 99    # 10人 x 1年・別の乱数
```

各人が実際に何回何をしたかを出す。**「DID NOTHING AT ALL」と出たら、そのテスターは何も試していない**＝失敗として扱う（黙って素通りするテスターを作らないため。実際に一度作った）。

4. 相棒の絵を描き直したら、配るサイズに落とす（原画は `companions/masters/`＝git 追跡外）。

```
python tools/build_frames.py
```

コマごとに「基準の絵からどれだけ動いたか」が出る。まばたき 0.1% 前後、小さな動き数%。二桁は別ポーズなので描き直す。

5. 手元で見る。

```
powershell -ExecutionPolicy Bypass -File tools/serve.ps1
```

## ファイルの見取り図

| ファイル | 中身 |
|---|---|
| `index.html` | 画面の入れ物。読むスクリプトに `?v=NN` が付く |
| `app.js` | 全画面の描画と操作 |
| `store.js` | 保存・検証・書き出し。ブラウザの中だけで動く「サーバー」。タブ間の同時書き込みは Web Locks で直列化 |
| `sw.js` | 圏外でも開けるようにする係。ページはネットワーク優先、それ以外は保存したものを出して裏で更新 |
| `manifest.webmanifest` | ホーム画面に追加したときの名前・アイコン・共有先登録 |
| `companions/frames/` | 相棒10体×3コマ（立ち姿・まばたき・小さな動き） |
| `design/` | Claude Design のアートボード（画面の正本） |
| `selftest.html` | この端末で本当に保存できるかを確かめるページ（実記録に触れない） |

## 公開

GitHub Pages。`main` の root をそのまま配る。**push は本人の手番。**
