# トレ録（toreroku・旧 おうちトレ）— このリポジトリの決まり

自宅でやったトレーニングを、その場で残しておくための記録帳。サーバーを持たない静的サイトで、記録は利用者の端末（IndexedDB）にだけ残る。**ここは公開して配るためのリポジトリ**で、ここに置いたものは全部そのまま人目に触れる。

もとは個人の記録を扱う別プロジェクトの中の一フォルダだった。**2026-09-11 に本人の判断で家を分けた**＝公開のたびに「出していいものだけを切り出す」作業が要るのは、母屋の中に置いているからだった。いまは切り出しが要らない。

## 絶対に守ること

- **個人データを1バイトも置かない。** 日記・体調・家計・位置・認証情報・実際の記録ファイル。ここは他人の端末に配られる。
- **外部へ通信を増やさない。** いまの外向きは3つだけ＝(1) 動画のサムネイル `i.ytimg.com`（利用者のブラウザが直接読む・保存しない）、(2) 貼られた URL のタイトルを取る YouTube oEmbed、(3) **利用者が自分の Gemini キーを設定に入れたときだけ**、動画の URL と読み取りの指示を Google の `generativelanguage.googleapis.com` へ送る（種目の読み取り・2026-09-11 に REST と CORS を実測・2026-09-12 実装）。記録は送らない。キーは端末内のみ・書き出しに入れない。**これ以外を足すときは、なぜ要るかを README に書いてからにする。** 解析タグ・外部フォント・CDN は入れない。
- **点数をつけない。順位をつけない。連続日数を数えない。励まさない。** 事実だけを淡々と出す。これは思想であって好みではない（[README](README.md) の「しないこと」）。
- **色相だけで意味を伝えない。** 作者は色弱。形・枠・明度差を必ず併用する。
- **名前は「トレ録」（2026-09-12 本人決定・公開スラッグ `toreroku`）。** 前の名前「おうちトレ」は、「うちトレ」がアプリの商標として登録済みと分かったため公開直前に取り下げ（本人判断）。「トレ録」の商標は別タスクで確認中。内部の綴り（DB 名・キャッシュ名 `ouchitore`）は互換のため据え置き。商標は 2026-09-12 に J-PlatPat・Toreru で調査済み＝同名の登録は無いが、**「うちトレ」が第9類（スマホアプリ）で登録済み（登録6853506・2034年まで）**。**名前は「トレ録」で確定・出願はしない（2026-09-13 本人決定）。** 再検討するのは、利用者が目に見えて増えたときか、有料化・広告など商用に踏み込んだときだけ。材料は作者の手元の記録にある。
- **画面の文言は「メニュー」単独を使わず「トレーニングメニュー」（2026-09-12 本人決定）。** 本人にとって「メニュー」はシステムのメニュー。例外＝ブラウザ自身のメニューを指す文（ホーム画面に追加の案内）。
- **比喩や造語で機能を呼ばない。** 「種目の一覧」は一覧であって「庫」ではない。
- **絵が言っていることを文字で繰り返さない（2026-09-11 本人）。** 相棒の絵はデフォルメしておらず、見ればその動物だと分かる。「犬や猫と分かる絵の下に イヌ ネコ なんて書かないでしょ」。同じ理由で、押せば記録される丸に「完了を記録」と書かない。**画面から消した言葉：相棒の動物名（選択画面）／「完了を記録」。** 読み上げ用の `alt` と `aria-label` は例外＝絵が見えない人には文字しか無い。
- **相棒に名前を付けない。** 絵だけ。既存の語・人名・名字と重ならない造語を探して二度失敗した（2026-09-11）。読み上げ用の `alt` と `aria-label` にだけ動物名が入る。
- **静かな一言の規則（2026-09-11 Claude Design 確定）。** 相棒は**見たことを言い、操作上必要なことにも触れてよい**（2026-09-13 本人「強制しなきゃいいんだから、操作上必要なことに触れることはなんらおかしくない」＝「次の行動に触れない」は撤廃）。触れてはいけないのは気持ちの代弁・急かし・褒め。出す場所はカードの上の1行だけ。**出してよい**＝時間帯の挨拶（呼び名は起動時の1回だけ）／はじめて開いた日／その日まだ記録が無い／久しぶり（何日前）／きょうの件数／今週の日数。**出さない**＝記録した直後（褒めになる。丸と数字だけが動く）／残り勘定（「あと1回で3日」）／気持ちの代弁／連続日数／ホーム以外の画面。**間合い**＝1日に1つ選んでその日は固定、同じ文は3日空ける。記憶は端末の中だけ（`localStorage`）で、書き出しファイルには入れない。
- **ただし、据え置いた一言が事実と違ってしまったら落とす（2026-09-12）。** 例＝朝に選ばれた「きょうはまだ書いていません」を、記録した後も出し続けない。褒めないことより、嘘をつかないことが先。落とすときは挨拶だけにする（何をしたかは相変わらず言わない）。
- **相棒は黙っているのではない（2026-09-11 本人の訂正）。** 約束は「そっと見守る・励ましたり煽ったりしない」であって、無言ではない。本人の言葉＝「強制したりおだてたりそういう能動的な問いかけ、発言はしない。静かに見守る。**静かな言葉はあっていいはずです**」。だから**静かな一言は置いてよい**（例＝メニューがまだ無い日の「まだメニューがありません。YouTube の動画の URL から、ひとつ作ってみませんか。」）。置けないのは、急かす・褒める・続きを迫る・感情を代弁する言葉。

- **押せない状態は `disabled` でなく `aria-disabled`。** ネイティブの `disabled` はフォーカスを外す実装があり、読み上げでその状態（例＝「〜は 07:20 につけました」）に到達できない端末がある。到達はできて、押しても何も起きない形にする。
- **OS で文面を変える場所がある。** 「7日間使わないと消える」は iOS（WebKit）だけの話で、Android で言うと嘘になる。`whichPhone()` を通す文面＝ホーム画面に追加の案内・ホームの警告バー・書き出し画面の注意。

## 見た目

レイアウト・配色・部品・遷移・コピーは **Claude Design** が決める。**新しいチャットで頼むときは、依頼の前に `design/SETTLED.md` をそのまま貼ること**（貼らないと、決まったことがゼロから考え直される。2026-09-10 に実際にそうなった）。ここでコードを書く側が画面を自作しない（毎回ダサくなる）。決まった見た目は `design/` のアートボードにある。寸法の制約（指で押せる大きさ等）は要件として Design に渡す。

## 変更のしかた

1. `index.html` の `?v=NN` を**必ず**上げる。上げないと、ホーム画面に追加した端末が古い版を持ち続ける。
2. データ層（`store.js`）を触ったら回帰テストを通す。

```
node tools/test_store.js
```

3. データ層を触ったら、12人分の使い方も流す（数週間ぶんの日付を一気に。実時間では出ない種類のズレが出る）。

```
node tools/simulate.js          # 12人 x 8週
node tools/simulate.js 52 99    # 12人 x 1年・別の乱数
```

各人が実際に何回何をしたかを出す。**「DID NOTHING AT ALL」と出たら、そのテスターは何も試していない**＝失敗として扱う（黙って素通りするテスターを作らないため。実際に一度作った）。

4. 相棒の絵を描き直したら、配るサイズに落とす（原画は `companions/masters/`＝git 追跡外）。

```
python tools/build_frames.py
```

コマごとに「基準の絵からどれだけ動いたか」が出る。まばたき 0.1% 前後、小さな動き数%。二桁は別ポーズなので描き直す。

5. 手元で見る。

```
powershell -ExecutionPolicy Bypass -File tools/serve.ps1
```

## ファイルの見取り図

| ファイル | 中身 |
|---|---|
| `index.html` | 画面の入れ物。読むスクリプトに `?v=NN` が付く |
| `app.js` | 全画面の描画と操作 |
| `store.js` | 保存・検証・書き出し。ブラウザの中だけで動く「サーバー」。タブ間の同時書き込みは Web Locks で直列化。トレーニングメニューは `tag`（分類・1つ）と `ord`（利用者の並び順）、種目は `skip`（ふだんは外す）を持つ |
| `sw.js` | 圏外でも開けるようにする係。ページはネットワーク優先、それ以外は保存したものを出して裏で更新 |
| `manifest.webmanifest` | ホーム画面に追加したときの名前・アイコン・共有先登録 |
| `companions/frames/` | 相棒10体×3コマ（立ち姿・まばたき・小さな動き） |
| `design/` | Claude Design のアートボード（画面の正本） |
| `selftest.html` | この端末で本当に保存できるかを確かめるページ（実記録に触れない） |

## 公開

GitHub Pages。`main` の root をそのまま配る。**push は本人の手番。**
