# 見た目3テーマ — Claude Design 決定（2026-09-14）

- Design プロジェクト＝ https://claude.ai/design/p/d0b40f6f-2d12-4529-95f4-33dd97fd0823 （ファイル `見た目3テーマ.dc.html`・`themes.css`）
- 取得手段＝Claude Code の `DesignSync`（`list_files` → `get_file`・projectId 上記）。ブラウザから .dc.html を落とす必要はない。
- `themes.css` は丸ごと `C:\toreroku\themes.css` に保存（正本）。`tokens.css` の後ろに読み込み、`html[data-theme]` で切替。既定 `paper`。

## テーマ名と意味

| data-theme | 名前（設定画面の文言） | 副文 | 見本の3色（地・カード・主ボタン） |
|---|---|---|---|
| classic | いまのトレ録 | 青みの灰色の地・白いカード | #eef1f6 / #fff / #1d5f9f |
| paper（既定） | 紙 | 生成りの地・クリーム色のカード | #efe9df / #fffdf8 / #1f6fb2 |
| night | 夜 | 暗い地・明るい文字 | #14181d / #1e242b / #f2a33c |

③「夜」の出どころ（Design の説明）＝Apple フィットネス（黒に近い地と一段明るいカードの明度差）／Nike Training Club（暗い地に暖色を1色だけ＝主ボタン #f2a33c）／Hevy・Strong（押せる物の色を1色に絞る＝リンク・ボタン・目盛りの塗りを同じ1色）。目盛り4段は色相を変えず明度だけ（#5b4621→#f2a33c）。

## 変数の使い分け（themes.css）

`--bg` 画面の地／`--bg-fade` 下端の被せ／`--card` カード／`--ink --body --sub --faint` 文字4段／`--line --line2` 罫線／`--line-strong` 意味を持つ枠（3:1）／`--color-action --on-action --color-action-press` 主ボタン／`--cell-bg --cell-line --cell-dash --cell-dash-future --today-ring` 週の目盛り／`--lv1..--lv4` 記録ありの4段／`--shadow-card`。書体は `--sans: system-ui, sans-serif` のみ。

## ホーム（3テーマ共通の形）

- 頭＝`padding:20px 20px 14px`・地は `--card`・下罫線 `--line`。日付 700 15px `--sub`。タイトル「トレ録」800 30px `--ink`。時計 700 20px `--body` tabular-nums（等幅フォントは使わない）。
- 週の目盛り＝7列 grid gap 4px。列＝`padding:6px 0 7px; border-radius:9px`。今日＝`border:2px solid var(--today-ring); background:var(--cell-bg)`・曜日 800 11px `--ink`。過去＝`1px solid var(--cell-line)`・曜日 700 `--sub`。未来＝枠なし・曜日 `--faint`・押せない。四角 26px 角丸7＝記録あり `--lv1..4`／なし `1.5px dashed var(--cell-dash)`（未来 `--cell-dash-future`）。
- 「今週の実績 **1** 日」（700 13px `--body`・数字 800 17px `--ink`）／右「先週 5 日」。
- きょう動いた分カード＝`--card`・角丸18・`--shadow-card`・`padding:16px 18px`・左に相棒の絵 60px・中央に見出し 800 12px `--sub` letter-spacing .14em →「9 種目 ｜ 6 分」（数字 800 44px `--ink`・単位 700 13px `--sub`・間に 1×30px の `--line`）→「1件 ／ 08:10」700 12px `--sub`。
- 「今日の記録」800 13px `--body`／右「1件 ／ 最後は 08:10」700 12px。記録カード＝題名 800 15px・「9種目 ／ 08:10 実施」700 12px・右にサムネ 112×64 角丸8・下段 border-top `--line`・「種目を見る ∨」700 13px `--color-action`。**「編集」は置かない（本人 2026-09-14「編集はないほうがよかった」＝SETTLED 9/12 の決定を維持）＝上段（題名・サムネ）全体が入口で行末に「›」20px `--sub`・開いた先で直す。**
- 「前の日の記録を見る ›」min-height 44・border-top・400 14px `--body`。
- 主ボタン二態＝高さ58・角丸16・800 17px letter-spacing .02em。0件＝塗り `--color-action` 文字 `--on-action`「記録する」。1件以上＝`border:1.5px solid var(--color-action)`・地 `--card`・`--shadow-card`・文字 `--color-action`・左に 20px の＋（SVG stroke 2.6 丸端）・文言 **「もう1つ記録する」**（Design 推奨。代案「続けて記録する」「別のものも記録する」・本人確認待ち）。

## 設定 → 見た目（3択の部品）

- 見出し「見た目」800 20px。説明「アプリ全体の色が変わります。文字の大きさや並び方は変わりません。」400 14px。
- 行＝min-height 72・角丸14・地 `--card`・`padding:14px 16px`・gap 14。未選択＝`1px solid var(--line-strong)`＋空の丸（24px・`2px solid var(--sub)`）。**選択中＝`2px solid var(--ink)` の枠＋丸が `--color-action` 塗りに ✓（stroke `--on-action` 3.2）＋右端の札「選択中」（800 11px・`1px solid var(--ink)`・角丸5・padding 5px 6px）**＝色相以外の3つで分かる。
- 丸の右に見本3色（各 16×26 角丸4・`1px solid var(--line-strong)`）→ 名前（選択中 800／他 700・16px `--ink`）＋副文（400 12px `--sub`）。
- 相棒の絵と動画サムネは Design 側は枠だけの仮置き。実装は実物を出す。

## 前の日の記録＝月のカレンダー（Claude Design 決定・2026-09-14・ファイル `過去の記録カレンダー.dc.html`）

- 出どころ＝Hevy（月のグリッドでやった日を塗る・押すとその日へ）／Strong・Stronglifts（やった日に印・押すとその日の記録へ）／Apple フィットネス（月のグリッドに各日を小さく並べ前後の月へ）。共通の4点だけ採用＝**月のグリッド／やった日だけ印／押すとその日へ／前後の月へ**。休養日数・連続・励ましは採らない。
- 頭＝「‹ 戻る」（44・`--color-action`）／題「前の日の記録」800 17px。月の行＝「‹ 前の月」700 14px／「2026年 9月」800 18px／「次の月 ›」（今月なら `--faint` で押せない）。
- 曜日行 月〜日（700 11px `--faint`・月曜始まり）。日のセル＝min-height 48・padding 5px 0 6px・角丸9・数字 700 13px・下に 22px 角丸6 の四角。**記録あり＝四角の塗り（1件 `--lv1`／2件 `--lv2`／3〜4件 `--lv3`／5件〜 `--lv4`）／記録なし＝`1.5px dashed --cell-dash`／未来＝`--cell-dash-future` で数字も `--faint`・押せない／今日＝セルに `2px solid --today-ring`／選んだ日＝セル地 `--ink`・数字 `--card`（反転）**。色相は使わない。
- 凡例＝`--lv1..4` の 12px 四角＋「記録の多い日ほど濃く」／破線の四角＋「記録なし」。次に1行「この月に記録があった日は N 日です。日を押すと、その日の記録が下に出ます。」
- 下＝「9月12日（土）の記録」800 15px＋右「2件 ／ 最後は 10:30」→ ホームと同じ記録カード（**「編集」は置かない＝9/12・9/14 決定を優先**・上段を押すと開く）。記録が無い日＝「9月13日（日）は記録がありません。」＋輪郭ボタン「**この日に記録する**」（ホーム2態目と同じ形・「足す」は使わない）。
- **動画の無い記録の絵（1パターン）＝2026-09-14 同日に本人是正「YouTube のサムネイルみたいなちゃんとした絵、イラストを」→ Codex image_gen で描いた「セルフトレ」文字入りイラスト（居間のマットでスクワットする**若くスリムで性別を特定しない後ろ姿**・「セルフトレ」は細めのゴシックで**1行**・16:9＝本人 2026-09-14 の是正4回：「男性か女性かわからない感じに」→「文字が太すぎ」→「リアルな中高年はやめて・うそでもいいから若くスリムに」→「折り返さず1行に」で v5 が確定）を `thumbs/selftre.png`（640×360・PNG）として採用。原本＝`C:/VoiceMirror/data/workout/thumbs/selftre-v5.png`（1672×941・v1〜v4 は同フォルダに保管）・依頼文＝`C:/VoiceMirror/handoff/codex_chat/toreroku-selftre-thumb.md`。読めない時だけ Design の「枠に四角」の SVG に戻る。**（Design 原案＝「枠に四角」の SVG（112×64・角丸8・地 `--line2`・36px の角丸枠と 18px の塗り四角を `--faint` で）。週の目盛りの「今日の枠」を小さくした形で、手入力・道具・場所を表さない。3テーマ共通・画像ファイル不要。Codex で絵を描く案は採らなかった（本人「おまかせ」・2026-09-14）。
- 手入力の記録の名前＝「セルフトレ」（本人案・同日採用）。

## 確定ボタン（Claude Design 決定・2026-09-14・`確定ボタン.dc.html` 案A）

本人「塗りの青いボタンは好きじゃない。業務アプリのボタンを思い出す」→ 塗りの板を全廃。確定＝高さ58・角丸16・800 17px はそのまま、`border:2.5px solid --color-action`＋地 `color-mix(--color-action 9%, --card)`＋`box-shadow:0 3px 0 --color-action-press`（下の段）。押下中は段を消して本体を3px下げる（形で沈む）。無効は枠と文字 `--faint`・段なし。任意の輪郭ボタン（1.5px・段なし）との差は枠の太さ・淡い地・段の3つ＝色相で分けない。ホーム0件の日の「記録する」も同じ形（塗りはアプリから無くなった）。案B（`--ink` の枠・主色不使用）は不採用（押せる色が2系統になる）。
